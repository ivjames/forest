{
  "name": "lost-in-the-forest-88-mobile",
  "version": "1.0.0",
  "description": "Capacitor shell for Lost in the Forest '88 (iOS / Android).",
  "author": "ivjames",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "sync": "node sync-game.mjs && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "run:android": "npm run sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
